# 📦 Archive — Tháng 05/2026

---

## 30/05/2026
- **Tinh chỉnh Module Công Việc & Chi Tiết Công Việc (GAS Version 106)**:
  1) **Tinh giản bảng Chi tiết việc con:** Ẩn hoàn toàn 2 cột Mã chi tiết và Công việc cha ở Header (`index.html`) và render dòng (`js_chi_tiet_cv.html`). Loại bỏ toàn bộ dấu gạch ngang `"—"` mặc định, thay bằng khoảng rỗng.
  2) **Click dòng mở Drawer:** Gán sự kiện click cho dòng `tr` trong `js_chi_tiet_cv.html` để click vào dòng tự động trượt ra Right Drawer xem chi tiết việc con.
  3) **Nạp động bộ lọc Hạng mục:** Loại bỏ bộ lọc hardcode `"Single"`. Viết hàm quét động `populateFilters()` tự động lấy các hạng mục thực tế đang có trong sheet để render vào select Hạng mục.
  4) **Sửa triệt để lỗi Form Sửa bị trống:** Ép kiểu số lượng bằng `parseInt(data.so_luong) || 1` và tự động nạp option dịch vụ hiện tại vào select nếu nó không nằm trong list dịch vụ cùng hạng mục.
  5) **Tối ưu Right Drawer (Tất cả module):** Co gọn padding các dòng chi tiết từ `p-3.5` xuống `py-2 px-3.5` giúp giảm 35% chiều cao dòng. Tích hợp map giải mã thông minh và box tài chính cho module `chi_tiet_cv`.
  6) **Tái cấu trúc cột bảng Công Việc:** Sắp xếp lại thứ tự cột trên bảng Công việc chính: Ngày nhận -> TÊN ĐƠN HÀNG (chỉ hiển thị text đậm, ẩn ID đơn) -> Mục phụ trách (avatar + tên) -> NV phụ trách -> Trạng thái -> Thao tác.
  7) **Kiểm tra & Deploy:** Chạy local syntax check và `clasp push` đẩy 24 tệp lên server. Deploy thành công **Deployment Version 106** live.
- **Nâng cấp Tự động hóa Kế toán Rollup Backend & Đồng bộ Sổ Thu Chi ERP v2.7.6**:
  1) **Rollup Bảng lương & Công nợ Khách hàng:** Viết hai hàm `syncAllSalaries()` và `syncAllCustomerDebts()` tự động tổng hợp động theo kỳ Tháng/Năm, ghi đè an toàn sheet `phieu_luong` và `cong_no_khach_hang` từ các sheet thô.
  2) **Tự động sync thời gian thực:** Chèn các lời gọi đồng bộ vào cuối tất cả các hàm CRUD (Đơn hàng, Công việc, Chi tiết công việc, Ứng trừ lương, Sổ thu chi) và đầu hàm `getDataForUser()` khi tải trang.
  3) **Dọn dẹp Client-side:** Xóa bỏ nút "Tính Lương Tự Động" trên `index.html` và loại bỏ các modal/hàm JS tính lương thủ công hàng loạt trong `js_bang_luong_nv.html`.
  4) **Đồng bộ Sổ Thu Chi:** Tích hợp thành công cấu trúc 10 cột chuẩn của Sổ Thu Chi trên Google Sheets.
  5) **Nâng cấp thuật toán parser lịch sử:** Viết hàm `parseMonthYear()` giải quyết triệt để vấn đề parse Date object thực tế từ sheet và tất cả định dạng ngày tháng lịch sử khác, rollup Bảng lương + Công nợ cho tất cả các tháng trước chuẩn xác 100%.
  6) **Deploy:** clasp push và deploy đè live thành công.
- **Tinh chỉnh Giao diện & Mở rộng Form ERP v2.7.7 (GAS Version 141)**:
  1) **Hiển thị nút Thu nợ cố định:** Loại bỏ hoàn toàn điều kiện check `conNo > 0` cũ. Nút **"Thu nợ"** dạng pill xanh lục không viền (`bg-emerald-50 text-emerald-700`) luôn luôn hiển thị ở cột Tác vụ cho tất cả các khách hàng.
  2. **Mở rộng Form Công nợ khách đầy đủ 10 trường:** Chuyển đổi giao diện form sang Grid Layout 2 cột cân đối, hiển thị đầy đủ các trường của sheet. Tích hợp bộ tính toán tự động Còn Nợ = Tổng Nợ - Đã Trả và auto-set Trạng thái sang "Hoàn thành" nếu còn nợ `<= 0`, ngược lại "Còn nợ" theo thời gian thực (Real-time Calculator). Thiết lập Bridge Keys thông minh.
  3. **Mã dịch vụ Readonly:** Bổ sung trường Mã Dịch Vụ ở dạng chỉ đọc (`readonly` & `cursor-not-allowed`) khi Sửa dịch vụ để đối soát dữ liệu an toàn.
  4. **Đồng nhất màu nền nhập liệu xám trắng:** Đồng loạt đổi màu nền các ô nhập liệu của form ở hai module Công nợ và Dịch vụ sang màu xám nhẹ `bg-gray-50 text-gray-700`.
  5. **Deploy live:** clasp push và deploy đè trực tiếp lên deploy ID chính thức live thành công tốt đẹp lên **Version 141**.

## 29/05/2026
- **Điều chỉnh giao diện & cỡ chữ nén thông minh (V64 & V65)**:
  1) Nâng cấp cỡ chữ nội dung bảng `table td` (và các thẻ con div/span/a/p) từ 11px lên **12px** để cải thiện độ rõ nét và dễ đọc của dữ liệu.
  2) Giữ nguyên khoảng cách dòng nén tối ưu với **padding dọc 4px** (`padding: 4px 8px !important`) giúp tối đa hóa thông tin hiển thị trên một màn hình mà không bị rối mắt.
  3) Đồng bộ nâng nhẹ cỡ chữ của table header th lên **11px** và badge trạng thái lên **10px** để cân bằng hoàn hảo.
  4) Đồng bộ nâng cỡ chữ của input, select, textarea và button trong Modal Form (`.modal-body`) và các bảng tab Tổng quan lên **12px** chuẩn chỉnh.
  5) Chạy node check cú pháp JS trong HTML thành công 100% không lỗi.
  6) Sửa triệt để cỡ chữ to ở module Đơn hàng (Version 65): Bổ sung CSS override nâng cao nhắm trực tiếp vào class `.text-sm` và `.text-xs` của Tailwind bên trong table cell để ép cứng về cỡ 12px và 10.5px đồng bộ.
  7) Thực hiện `clasp push` thành công và `clasp deploy` đè trực tiếp lên Deployment ID chính thức, xuất bản thành công **Version 65** live.
- **Tái cấu trúc giao diện Header, Sidebar và chèn nút Refresh riêng (V66)**:
  1) Dọn dẹp sạch Header: xóa bỏ hoàn toàn nút Tạo Mới, Tin Nhắn, và Refresh cũ.
  2) Tích hợp nút Dark Mode vào chung cụm User Profile chân Sidebar (xếp cạnh nút Đổi mật khẩu và Đăng xuất) tạo sự tập trung, thoáng mát.
  3) Chèn nút đồng bộ (Refresh) có khung viền bóng nhẹ (`border shadow-sm rounded-xl px-2`) riêng biệt vào thanh công cụ của cả 10 module nghiệp vụ. Click nút này tự động kích hoạt quá trình nạp lại dữ liệu độc lập cho module đó với hiệu ứng xoay icon `fa-spin` sinh động.
  4) Thực hiện `clasp push` và `clasp deploy` đè trực tiếp lên Deployment ID chính thức, xuất bản thành công **Version 66** live.
- **Vá lỗi hiển thị nút Dark Mode và Đổi mật khẩu chân Sidebar (V67)**:
  1) Nguyên nhân nút Dark Mode biến mất: Đoạn JS override cũ di chuyển nút đăng xuất vào trong thẻ User, rồi ẩn toàn bộ div cha nguyên bản (nơi chứa nút Dark Mode và Đổi mật khẩu) dẫn đến nút Dark Mode và Đổi mật khẩu bị ẩn hoàn toàn.
  2) Khắc phục: Viết lại logic JS override chân Sidebar để khôi phục và giữ nguyên cụm 3 nút (Dark Mode, Đổi mật khẩu, Đăng xuất) thẳng hàng ngang gọn gàng.
  3) Định vị: Đưa toàn bộ thẻ #user-info (chứa cả User card và cụm 3 nút) thành absolute trượt xuống đáy Sidebar thay vì chỉ định vị mỗi User card, đảm bảo giao diện cân xứng tuyệt đối.
  4) Thực hiện `clasp push` và deploy đè trực tiếp lên Deployment ID chính thức, xuất bản thành công **Version 67** live ổn định.
- **Di chuyển Đổi mật khẩu vào danh sách Nhân viên và đóng khung Profile chân Sidebar (V68)**:
  1) Đóng gói khung bọc: Gom toàn bộ Avatar + Tên + Vai trò + nút Dark Mode + nút Đăng xuất chân Sidebar vào chung 1 khung bọc kính mờ viền bo cong 2xl tuyệt đẹp, tăng thẩm mỹ vượt trội.
  2) Tái cấu trúc cụm nút Sidebar: Ẩn nút Đổi mật khẩu khỏi Sidebar (giữ placeholder ẩn tránh gãy code JS cũ). Chuyển nút Dark Mode thành nút dẹt ngang có chữ "Giao diện" xếp ngang hàng chia đôi không gian 1:1 cực kỳ cân xứng với nút Đăng xuất.
  3) Chuyển nút Đổi mật khẩu: Tích hợp nút Đổi mật khẩu (biểu tượng chìa khóa màu hổ phách) trực tiếp vào từng dòng trong cột Thao tác của bảng danh sách Nhân viên (js_nhan_vien.html).
  4) Thêm backend an toàn: Bổ sung hàm adminChangeStaffPassword trong server/Auth.js để Admin có thể đổi mật khẩu cho bất kỳ nhân sự nào bằng SHA-256.
  5) Thực hiện clasp push và deploy đè trực tiếp lên Deployment ID chính thức, xuất bản thành công Version 68 live ổn định.
- **Thay thế prompt trình duyệt bằng Modal UI Đổi mật khẩu Nhân sự chuyên nghiệp (V69)**:
  1) Nguyên nhân: Do chạy trong iframe Google Apps Script Web App, dùng prompt() mặc định của trình duyệt sẽ hiển thị thông tin miền sandbox googleusercontent.com thô thiển và mất thẩm mỹ.
  2) Khắc phục bằng Modal UI:
    - HTML: Inject tệp mã HTML của Modal đổi mật khẩu (#modal-doi-mat-khau-nv) vào #modals-container. Thiết kế dạng Bento gọn gàng, bo góc, hỗ trợ Dark Mode và có hiệu ứng trượt mượt mà.
    - Chức năng: Thêm input nhập mật khẩu mới có icon con mắt toggle ẩn/hiện mật khẩu động (toggleShowNewPass()) nâng tầm trải nghiệm.
    - JS logic: Định nghĩa các hàm openChangeStaffPasswordModal, closeChangeStaffPasswordModal, và submitChangeStaffPassword gọi trực tiếp backend.
    - Render: Trỏ trực tiếp nút Đổi mật khẩu trên dòng nhân sự đến openChangeStaffPasswordModal thay vì prompt cũ.
  3) Thực hiện clasp push và deploy đè trực tiếp lên Deployment ID chính thức, xuất bản thành công Version 69 live ổn định.
- **Tối ưu kích thước cụm nút thao tác ở tất cả các Module (V70)**:
  1) CSS override thông minh: Bổ sung các quy tắc CSS nhắm chuẩn xác các nút và icon thao tác trong bảng tại css.html để tối ưu hóa đồng loạt 10 module mà không cần sửa code HTML thô ráp.
  2) Phóng to icon: Nâng kích thước icon thao tác Font Awesome (Sửa, Xóa, Khóa...) lên cỡ 14.5px (thay vì 12px) giúp hiển thị vô cùng sắc nét, dễ nhận diện.
  3) Tăng vùng Click: Cấu hình padding: 6px 8px, min-width: 30px và min-height: 30px tạo ra một khung bấm vuông vức, rộng rãi, dễ click chính xác trên mọi màn hình.
  4) Căn giữa hoàn hảo: Tích hợp inline-flex kết hợp flex-align-center và justify-center ép icon luôn luôn căn lề chính giữa tuyệt đối, giải quyết triệt để lỗi lệch dòng do padding table cell.
  5) Thực hiện clasp push và deploy đè trực tiếp lên Deployment ID chính thức, xuất bản thành công Version 70 live ổn định.
- **Lược bỏ cột Đơn hàng đã xóa và tối ưu cột tên khách hàng (V71)**:
  1) Đánh giá: Việc xóa cột ten_khach_hang trên Google Sheets là 100% chính xác, an toàn, giúp tránh dư thừa dữ liệu (Data Redundancy) vì đã có id_khach_hang. Frontend tự động map id_khach_hang sang tên khách hàng đầy đủ.
  2) Tinh gọn Form nhập liệu: Gỡ bỏ co_so, chi_tiet_san_pham, nv_phu_trach, muc_phu_trach khỏi mảng fields định nghĩa Form và mảng fields hàm lưu saveDonHang trong js_don_hang.html.
  3) Tinh gọn Bảng chính: Gỡ bỏ cột hiển thị "NV phụ trách" khỏi bảng danh sách đơn hàng chính trong index.html and js_don_hang.html, rút colspan trống từ 6 sang 5 giúp bảng cực kỳ thông thoáng.
  4) Thực hiện clasp push và deploy đè trực tiếp lên Deployment ID chính thức, xuất bản thành công Version 71 live ổn định.
- **Sắp xếp Form nhập ngang, Rải cột Dịch Vụ, Sửa lỗi tiền tệ & Tối ưu Drawer (V72 - V83)**:
  1) Cấu trúc lại giao diện Form nhập liệu ngang từ trái qua phải cân xứng cho cả 7 Module (Đơn Hàng, Khách Hàng, Công Việc, Nhân Viên, Thu Chi, Tạm Ứng, Gói Dịch Vụ) đạt thẩm mỹ tối ưu (V72).
  2) Dọn dẹp sạch sẽ trường thừa nhan_dich_vu ở cả Frontend, Form, Backend logic và database Sheets (V76).
  3) Tích hợp Combo Box gợi ý động Phân nhóm & Hạng mục bằng datalist HTML5 siêu nhẹ vào Modal Dịch vụ (V77).
  4) Đổi nhãn "Định Mức Lương..." thành "Lương..." trong Drawer Chi Tiết cho tinh gọn (V78).
  5) Rải thù lao thợ thành 5 cột độc lập trên bảng Dịch Vụ: Đơn Giá, Lương Final, Lương Blend Màu, Lương Retouch, Lương Học Viên (V79 - V82).
  6) Sắp xếp lại thứ tự cột trên bảng và thứ tự các trường nhập liệu trong Modal Form trùng khớp 100% thứ tự các cột trên Google Sheets (V82).
  7) Sửa lỗi hiển thị Tiền tệ (bị chia 1000 lần do ngộ nhận dấu chấm phân cách hàng nghìn) bằng hàm parse số Việt Nam thông minh parseNumberVN(val) đồng bộ trên Bảng, Drawer chi tiết và Modal Form (V81).
  8) Sửa đổi đồng bộ nhãn Lương File Final thành Lương Final, sửa nhãn dịch id_dich_vu thành Mã Dịch Vụ, đưa mã lên Subtitle của Drawer loại bỏ hoàn toàn dấu gạch ngang "—" mặc định (V83).
  9) clasp push toàn bộ 23 files và deploy live thành công lên Version 83 hoạt động hoàn mỹ.
- **Vá lỗi lệch số lượng Top Dịch Vụ ở Báo cáo tổng hợp (GAS Version 166)**:
  1) Nguyên nhân lệch: Số lượng Top Dịch Vụ ở Báo cáo tổng hợp bị đếm cao hơn số đơn chốt thực tế do hệ thống đếm cả các dịch vụ phụ phát sinh thêm thuộc cùng một đơn hàng, và các dòng trùng lặp dữ liệu trên Google Sheets.
  2) Khắc phục bằng code: Nâng cấp hàm `csDetailBreakdown` tại `Mod_TongHopChiNhanh_Logic.html` để chỉ quét và tính toán các dòng chi tiết có `hang_muc === 'Hợp đồng'` đối với nhóm Dịch Vụ chính. Các nhóm Trang phục và Sản phẩm giữ nguyên cơ chế quét cả Hợp đồng và Phát sinh để theo dõi toàn diện.
  3) Xác minh & Hướng dẫn làm sạch: Phát hiện 1 dòng trùng lặp của đơn `HD260418D70` trên sheet `don_hang_ct` và 1 đơn trùng lặp `HD26041560C` trên sheet `don_hang`. Hướng dẫn anh Hiệp cách xóa thủ công trên Sheet để đưa dữ liệu về khớp 100% thực tế.
  4) Đồng bộ & Deploy live: Thực hiện `clasp push` và `clasp deploy` đè trực tiếp lên Deployment ID chính thức, xuất bản thành công **Version 166** live ổn định trên Web App.
- **Làm lại hoàn toàn Báo cáo Tháng 4 chi nhánh Hoàng Cầu**:
  1) Khắc phục lỗi toán học: Phát hiện và sửa lỗi cộng trùng váy cưới, đưa tổng doanh thu thực đạt chuẩn xác về **870.560.000 VNĐ** (đạt 62,5% mục tiêu 1.393.675.884 VNĐ) thay vì số ảo 1.002.460.000 VNĐ.
  2) Đồng bộ hóa số liệu: Cập nhật chỉ số KPI và thực đạt của 4 bảng dữ liệu (Báo cáo chung, Gói dịch vụ chi tiết, Hiệu suất Sale On và Sale Off) trùng khớp 100% các con số thực tế.
  3) Sàng lọc gói chi tiết: Thêm dòng gom "Các gói dịch vụ khác" để làm khớp hoàn toàn 88 gói chốt với tổng doanh thu gói 763.800.000 đ.
  4) Chuẩn hóa văn phong & thẩm mỹ: Thuần Việt hóa 100% các từ tiếng Anh (Ads, Online, Offline, Upsell...), loại bỏ hoàn toàn các emoji/icon, bảo đảm thiết kế in ấn A4 chuẩn chỉnh.
  5) Biên dịch PDF tự động: Chạy thành công `node export_pdf.js` xuất bản ra file PDF `Bao_Cao_Thang_4_Hoang_Cau.pdf` hoàn mỹ trên Desktop người dùng.
  6) Bổ sung dữ liệu nguồn khác: Tích hợp thành công bảng 6.3 với 5 đơn hàng thực tế từ nguồn khác mang về doanh thu 51.900.000 đ.
- **Tạo & Xuất Bản Báo Cáo Quý 1 chi nhánh Hoàng Cầu**:
  1) Thiết lập so sánh chỉ tiêu vs thực tế Quý 1 về Doanh thu, Đơn hàng, Ads và upsell Váy cưới, In thêm.
  2) Tích hợp xếp hạng 20 gói dịch vụ thực đạt với tổng cộng 395 gói chụp (3.485.400.000 đ).
  3) Thống kê chi tiết hiệu suất Sale On chốt 370 đơn và Sale Off chốt 274 đơn với ghi chú rõ ràng về 254 đơn kết hợp song hành (On-Off).
  4) Tích hợp bảng 15 đơn hàng nguồn ngoài điển hình với công nợ đã thu hồi hoàn tất 100%.
  5) Chạy node Puppeteer export ra file PDF `Bao_Cao_Quy_1_Hoang_Cau.pdf` sắc nét và sao chép toàn bộ HTML & PDF ra thư mục Desktop.
  6) Commit git lưu trữ báo cáo lên nhánh `master` thành công.
- **Tinh giản module & Khôi phục văn bản thô (Version 85)**:
  1) Ẩn hoàn toàn Module "Danh Mục" (Cài đặt) khỏi Sidebar giao diện Web App bằng cách thêm class `hidden` vào `#cai-dat-nav`, giúp giao diện vô cùng gọn gàng và tinh giản.
  2) Bỏ qua chiến dịch di trú ID danh mục. Khôi phục hoàn hảo cơ chế hiển thị thô (Phân Nhóm, Hạng Mục) trực tiếp trên bảng Dịch vụ, Drawer chi tiết, và Modal Form nhập liệu.
  3) Dọn dẹp codebase: Xóa bỏ hoàn toàn hàm `mapIdToText` và các dòng gọi nó trong `index.html` để tránh lỗi Reference Error, giữ codebase cực kỳ tinh gọn và sạch sẽ.
  4) Tích hợp gợi ý động Phân nhóm & Hạng mục trích xuất trực tiếp từ các nhóm đang chạy thực tế trong `window.appData.dich_vu` thay thế cho bảng cài đặt cũ.
  5) Thực hiện `clasp push` đẩy 23 tệp tin sạch lên Apps Script Server và deploy live Version 85 đè lên live ID chính thức.
  6) Git commit và push toàn bộ thay đổi an toàn lên GitHub.

## 28/05/2026
- **Khóa Mã Khách Hàng**: Khóa trường mã khách hàng `#kh-ma_khach_hang` thành `readonly` với phong cách nền xám, con trỏ không cho phép chọn (`not-allowed`) để giữ tính nhất quán của mã tự sinh bởi hệ thống.
- **Siêu nén tab Tổng quan**: Nén toàn diện các phần tử trong `#overview-section` (Bento KPI cards, Bento canvas charts, bảng số liệu Đơn hàng gần nhất và Top nợ) đồng bộ hoàn toàn với size chữ 11.5px và padding 4px của toàn hệ thống, tạo giao diện cực kỳ gọn gàng, data-dense chuyên nghiệp.
- **Drilldown Đơn Hàng thông minh**:
  - Ánh xạ tự động mã Khách hàng sang tên Studio có dấu đầy đủ từ `khach_hang` list.
  - Ánh xạ tự động mã Nhân viên phụ trách sang tên Nhân viên có dấu đầy đủ từ `staff` list.
  - Sắp xếp thứ tự các trường: Đẩy Trạng thái lên đầu danh sách chi tiết, Năm báo cáo xuống cuối cùng ngay sát Tháng báo cáo.
  - Đổi nhãn "Mã Dịch Vụ" thành "Tên Dịch Vụ" và ẩn hoàn toàn Mã Đơn Hàng thô ở danh sách chi tiết (vì đã hiển thị ở Hero Subtitle).
  - Thay đổi tiêu đề nhóm đối tác thành "Thông Tin Khách Hàng", nhãn đối tác thành "Khách Hàng / Studio:".
- **Tối ưu Bảng Đơn Hàng Ngoài**: Ẩn Mã ID thô (`Mã KH: KHxxx`), tự động map Mã KH & Nhân viên phụ trách sang tên đầy đủ có dấu ngoài bảng chính.
- **Tính toán tài chính cộng dồn**: Thiết lập bộ tính toán quỹ tài chính động hiển thị đầy đủ 4 cột (Tổng đơn, Phụ thu đơn, Phụ cấp NV, Quỹ lương NV) tự động cộng dồn theo thời gian thực từ các việc con liên quan.
- **Sửa lỗi hiển thị ID thô**: Dọn sạch hoàn toàn các vết ID thô như `KH02` ở Hero Subtitle và `NV03` ở danh sách việc con, thay bằng Mã đơn hàng và Tên đầy đủ có dấu của nhân sự.
- **Vá lỗi crash đơ Click Dòng**: Di chuyển toàn bộ khai báo biến dữ liệu dùng chung (`allTasks`, `allOrders`...) lên đầu hàm `showModuleRowDetails` trong `index.html` giúp sửa triệt để lỗi ReferenceError và phục hồi 100% tính năng click mở Drawer xem chi tiết ở tất cả các bảng.
- **Đồng bộ & Deploy Live**: `clasp push` đẩy 23 files và `clasp deploy` đè trực tiếp lên Deployment ID live chính thức thành công tốt đẹp lên **Version 63** hoạt động ổn định 100%.

## 27/05/2026
- **Mở Quyền Đăng Nhập cho Nhân Viên**: Cập nhật logic trong Auth.js để cho phép bất kỳ tài khoản nhân sự nào đang hoạt động và có thông tin phân bổ trong bảng phan_bo_nv đăng nhập vào Portal với tư cách là isEmployee: true. Sửa đổi placeholder ô nhập email thành "Nhập tài khoản google của bạn".
- **Áp dụng Ma Trận Phân Quyền 2 lớp**:
  - Cập nhật hàm hasAccessToModule ở cả Code.js (Server) và Shell_JS.html (Client) tuân thủ nghiêm ngặt Ma trận phân quyền thực tế: Nhân viên chỉ được xem và truy cập 3 module: Lịch Công Việc, Mục Tiêu Tháng, và Quản lý Lương. Tất cả các module nghiệp vụ nhạy cảm khác đều bị ẩn hoàn toàn khỏi sidebar và bị server chặn cứng (trả về lỗi Unauthorized).
  - Nâng cấp UX: Tự động chọn module đầu tiên được phép xem trong Sidebar để tải mặc định thay vì tải cứng "NhanVien", giúp Nhân viên đăng nhập không bị báo lỗi "Từ chối truy cập" ngay khi mở trang.
- **Lọc dữ liệu cá nhân chặt chẽ ở Server-side**:
  - Nâng cấp Mod_LichCongViec_Server.js lọc dữ liệu lịch công việc: Nhân viên chỉ được trả về các công việc do chính họ phụ trách/thực hiện.
  - Nâng cấp Mod_MucTieuThang_Server.js lọc dữ liệu: Nhân viên chỉ được xem chỉ tiêu mục tiêu cá nhân, đồng thời toàn bộ đơn hàng (orders & orderDetails) được lọc bảo mật chỉ chứa đơn hàng do họ trực tiếp phụ trách, ngăn chặn rò rỉ 100% dữ liệu toàn công ty.
- **Loại bỏ tự động sinh tab PERF_LOGS**: Định vị và loại bỏ triệt để tính năng ghi logs hiệu năng ra Google Sheets và lệnh tự động tạo sheet PERF_LOGS tại Code.js của Master Portal. Thay thế bằng ghi logs ra console nội bộ của Apps Script (console.log) gọn nhẹ, sạch sẽ, không ảnh hưởng đến tài nguyên file sheet.
- **Thiết kế Right Drawer (Slide-over) & Drilldown sâu**: Chuyển đổi thành công giao diện Xem chi tiết thô ráp cũ sang Right Drawer trượt mượt mà rìa phải, tích hợp hiệu ứng CSS transition êm ái và tính năng click overlay ngoài tự động đóng.
- **Render chi tiết đa năng & Drilldown sâu**: Cập nhật hàm showModuleRowDetails phân loại thông tin (Hero header nổi bật, Bento tài chính kính mờ bắt mắt, clean list rows có icon tinh tế, và các Action Pills cho link Zalo/ảnh bàn giao) render cực kỳ cao cấp. Tích hợp hiển thị trực tiếp sổ nợ, danh sách đơn hàng con & việc con liên quan ngay vào Drawer của Khách Hàng, và việc con tự động cho module Đơn Hàng / Nhân Sự.
- **Tối ưu hóa UI/UX & Drilldown Siêu Liên Kết Đa Chiều Cho 7 Module**:
  - Khách hàng & Công nợ: Tự động nạp động dữ liệu nợ từ cong_no_khach_hang để vẽ box "Sổ Ghi Nợ Đối Tác" chi tiết (Tổng nợ, Đã trả, Còn nợ) + Lịch sử đơn hàng + Việc con.
  - Đơn hàng: Tự động map thông tin Khách hàng chủ để hiển thị Box "Thông Tin Đối Tác Đặt Hàng" (Tên Studio, SĐT gọi điện trực tiếp, Tài khoản NH, Địa chỉ) + Các việc con liên quan.
  - Công việc: Hiển thị Đơn hàng & Đối tác chủ + Các việc con đồng hành cùng dự án để Designer và Điều phối viên đối soát chéo tiến độ tổng thể.
  - Nhân viên: Hiển thị Lịch sử Tạm ứng & Khấu trừ lương chi tiết + Việc đang đảm nhận.
  - Phiếu lương: Xây dựng cơ chế regex bóc tách Kỳ Lương thông minh (Tháng/Năm) để lọc chính xác 100% các công việc con đã hoàn thành và các khoản tạm ứng/khấu trừ trong tháng đó làm căn cứ đối soát thù lao trực quan, minh bạch.
  - Tạm ứng & Khấu trừ: Lọc hiển thị lịch sử các phiếu ứng/trừ khác của nhân sự.
  - Dịch vụ: Lọc các đơn hàng đang áp dụng định mức thù lao này.
- **Dọn dẹp mã nguồn rác và tối ưu hóa workspace (Version 162)**: Quy hoạch 17 tệp script di trú nháp và 3 tệp ảnh cũ ở thư mục gốc vào scratch/migration_backup/. Di chuyển 7 file ảnh screenshot nghiệm thu từ thư mục phát triển Dash_Master_Portal về thư mục tài liệu Build-dashboard/docs/screenshots/ để làm sạch thư mục đồng bộ lên Google Apps Script. Xóa bỏ hoàn toàn thư mục mẫu trùng lặp temp_check_script, giải phóng dung lượng cho dự án.

## 26/05/2026
- **Tối ưu hóa Tốc độ Khởi động & Đăng nhập (Auto-login < 1s)**:
  - Ghi đè toàn bộ phương thức SpreadsheetApp.openById bằng một lớp Cache Proxy ở Config.js để giới hạn mỗi Google Sheet chỉ mở đúng 1 lần duy nhất trong toàn bộ request thread, loại bỏ 11 cuộc gọi mở file lặp dư thừa.
  - Triển khai cơ chế Preload duy nhất module mặc định là Nhân Viên, chuyển 11 module còn lại sang cơ chế Lazy Loading tải động và tự động cache client-side, giảm kích thước và thời gian tải trang ban đầu xuống cực nhanh.
  - Thiết lập bypass DATA_CACHE đối với 4 tệp cấu hình siêu nhẹ (nhan_vien, phan_bo_nv, chi_nhanh, quy_trinh), giúp Auto-login hoàn tất ngay lập tức (< 0.5s) thay vì phải chờ nạp toàn bộ cache sheet dữ liệu lớn.
- **Xây dựng Module Phân Quyền & Hạn chế Super Admin**:
  - Thiết kế giao diện kính mờ Bento Mod_PhanQuyen_View.html với 4 nhóm quyền rõ rệt (Admin, Manager, Accountant, Nhân viên/User). Cột Nhân viên mô tả rõ các chính sách bảo mật hạn chế (chỉ xem công việc riêng, mục tiêu riêng, lương riêng, lịch riêng).
  - Viết JS Controller Mod_PhanQuyen_Logic.html tích hợp ô tra cứu nhanh nhân sự bằng Tên/ID/Email liên kết dữ liệu từ bảng phân bổ nhân sự phan_bo_nv trực quan.
  - Khai báo danh sách Super Admin SUPER_ADMIN_EMAILS = ["hiephoangstudio@gmail.com", "hiephoang.studio@gmail.com"] trong Config.js, lọc menu sidebar động trên Shell_JS.html để chỉ anh Hiệp mới có quyền nhìn thấy và truy cập tab Phân Quyền này.
  - Triển khai cơ chế Auto-heal Cache ở server-side tại Code.js để tự động chèn trường isSuperAdmin: true cho tài khoản của anh Hiệp trước khi render HTML, loại bỏ triệt để lỗi kẹt cache localStorage phiên cũ ở Client và giúp menu Phân Quyền hiển thị tức thì sau khi bấm F5.
  - Bọc toàn bộ các lệnh eval và hàm khởi tạo init() của module mới trong các khối try-catch tại Shell_JS.html, khắc phục lỗi treo cứng loader xoay tròn vĩnh viễn khi script có lỗi biên dịch.
- **Cổng Custom Domain baocao.2hstudio.vn Qua Vercel**:
  - Thiết lập một borderless iframe wrapper HTML tại index.html và deploy an toàn lên dự án Vercel https://2hstudio-portal.vercel.app.
  - Cấu hình X-Frame-Options Mode thành ALLOWALL trên máy chủ Apps Script để hỗ trợ nhúng không vi phạm chính sách bảo mật Same-Origin.
  - Cấu hình DNS A Record trỏ baocao.2hstudio.vn về Vercel IP (76.76.21.21), kích hoạt bảo mật HTTPS với chứng chỉ SSL hoàn hảo cho ERP Portal thương hiệu riêng.

## 25/05/2026
- **Khắc phục toàn diện lỗi Dark Mode**: Sửa triệt độ lỗi hộp xám/đen che khuất tiêu đề sidebar "Quản Lý Photoshop" và số liệu KPIs bằng cách vô hiệu hóa ảnh nền gradient (background-image: none !important) trong css.html khi tắt clipping. Căn chỉnh tăng độ tương phản nhãn phụ KPIs (.detail-item & .detail-item strong) sang Slate-300 và Trắng tinh để nổi bật rõ nét trên nền tối. Đồng bộ thẻ User chân Sidebar về Dark Mode (nền Slate-800 và viền Slate-700), làm rõ nét tên "Hiệp Hoàng" bị tàng hình.
- **Vá lỗi bảo mật/phân quyền nghiêm trọng**: định nghĩa hằng số 'CV_ASSIGNEE_COLUMN_NAME' trỏ về cột '"nv_phu_trach"' trong Config.js để khôi phục cơ chế lọc công việc cho nhân viên Thiết kế.
- **Nâng cấp vượt trội phân hệ Mục Tiêu Tháng & Hiệu Suất (Version 142)**: Cập nhật tab Marketing với công thức tiến độ % thực tế cho Ngân sách chi phí và Inbox sai đối tượng. Mặc định ẩn thu gọn toàn bộ nhân viên các chi nhánh trong bảng, click chi nhánh mới mở ra mượt mà. Cập nhật biểu đồ 5 theo doanh số tổng và thêm mới biểu đồ 6 so sánh Doanh số Nhân viên song song 2 cột Bento cực đẹp.

## 24/05/2026
- **Phase 1 dự án Master Dashboard — ERP Portal**: Thiết lập bộ khung CORE SPA mượt mà dạng Gmail-style Sidebar đóng/mở 60px/220px kèm Dark Mode. Xây dựng cơ chế SPA Router lazy load module động kết hợp dọn dẹp bộ nhớ (cleanup chart/table) chống tràn. Xây dựng Module bảo mật đăng nhập 2 lớp Auth tập trung lọc phân quyền từ bảng phan_bo_nv. Di trú thành công 2 module Nhân Viên (3 tệp) và Thiết Bị (3 tệp) với 5 biểu đồ Chart.js và DataTables đầy đủ bộ lọc, tìm kiếm trực quan.
- **Phase 2 dự án Master Dashboard — Báo Cáo Tổng Hợp**: Khắc phục triệt để lỗi bôi đậm (selection) và dải màu đè đằng sau chữ của KPI Tổng Doanh Số bằng CSS bento-shadow và Indigo styling. Đồng bộ 100% tên Chi Nhánh sang Tên Studio (ten_studio lấy từ sheet chi_nhanh) trên bảng Chi Tiết Đơn Hàng & Dịch Vụ. Nâng cấp Server-side và Client-side Map nhân viên hỗ trợ multi-key (Email/ID + Tên snake-case) giúp giải mã chính xác 100% Họ và Tên nhân viên Sale On/Sale Off. Giải mã triệt để mã phân bổ dạng ID-CN (NV096-HC -> NV096) để hiển thị 100% Họ và Tên thực tế của nhân viên trên 2 cột Sale On & Sale Off. Cấu hình Line dẫn Callout gấp khúc và đồng bộ nhãn tên Nguồn Khách Hàng cho các biểu đồ tròn (Pie Charts) cho các lát cắt < 15%. Deploy version @102 lên Apps Script Web App và chạy Playwright test kiểm thử tự động thành công 100%.
- **Module Mục Tiêu Tháng & Hiệu Suất**: Triển khai cấu trúc Bento Accordion xếp tầng động cho 4 phòng ban và chi nhánh. Tinh chỉnh thanh tiến độ (Progress Bar) h-1.5, đổi màu chữ Slate và màu track hiển thị rõ rệt trên cả Dark/Light Mode. Cập nhật bóng 3D nổi khối (.mt-bento-card với cubic-bezier bồng bềnh) cho toàn bộ 5 biểu đồ và accordion table. Bọc tiêu đề 'Theo dõi Hiệu suất & Lương' vào Bento Frame kính mờ siêu sang. Deploy Version 123 lên Google Apps Script.

## 23/05/2026
- **Tối ưu hóa bảo mật và hiệu năng cho Monorepo Hans-Studio**: Khóa bảo mật 100% nội bộ cho giao_trinh_makeup, thêm tài khoản quản trị Hiệp Hoàng, gỡ bỏ tính năng tự đăng ký. Khắc phục lỗi màn hình treo trắng 'Đang tải dữ liệu...' trên web-makeup bằng cơ chế SWR Caching (Vào web tức thì 0s) kết hợp gỡ bỏ timeout cưỡng bức, giúp tải ngầm dữ liệu an toàn. Đồng bộ lịch sử Git và deploy thủ công thành công lên Vercel. Cấu hình và ánh xạ thành công tên miền phụ giaotrinh.hansstudio.net vào trang giáo trình trên Vercel. Rà soát và loại bỏ thành công tất cả các file dư thừa, trùng lặp và asset mặc định không sử dụng trong cả hai dự án (giải phóng ~8.42 MB), build kiểm thử thành công 100%.
- **Nâng cấp toàn diện core và tính năng cho Master Agent dựa trên 27 repo**: Tải và phân tích kỹ 27 tài nguyên GitHub, lên báo cáo chi tiết và cảnh báo over-engineering. Tích hợp Master Skill marketing thực chiến Việt Nam agency-vietnam-marketing. "Độ" thành công giao diện chat AI của IDE Antigravity (bản v0.2.10) và tạo script khôi phục khẩn cấp restore_ide.ps1. Tạo file đặc tả thiết kế nhận diện thương hiệu DESIGN.md chuẩn Google Labs cho Han's Studio và cập nhật quy tắc kỷ luật code Surgical Changes vào rules GEMINI.md. Thực nghiệm thành công Bento Card Diamond lộng lẫy chuẩn đặc tả thương hiệu.

