# 📦 Archive — Tháng 06/2026

---

## 21/06/2026
- **Tùy chỉnh Confirm Dialog cao cấp**: 
  - Khắc phục nhược điểm thẩm mỹ của hàm `confirm()` mặc định của trình duyệt. 
  - Thiết kế và triển khai Custom Confirm Modal dạng overlay kính mờ màu tối kết hợp viền vàng gold sang trọng của studio.
  - Chuyển đổi các chức năng quan trọng (`deleteFolder`, `deleteSlide`, `resetToDefault`) sang bất đồng bộ để tương tác hoàn hảo với modal Promise-based mới.
- **Tinh chỉnh Spacing & Căn lề đối xứng**:
  - Căn chỉnh viền khung ảnh bên trái (`#card-photo-side-container`) khớp đối xứng hoàn hảo với viền khung chứa nội dung bên phải (thụt lề 30px).
  - Nén khoảng cách dọc của các phần tử trong thẻ báo giá (logo, tên gói, giá cả, danh sách) đi 30% để giúp bố cục gọn gàng, tăng breathing room cho toàn trang.
- **Tối ưu hóa Phím tắt (Shortcuts) danh sách**:
  - Loại bỏ hoàn toàn các nút thao tác thủ công rườm rà dưới các danh sách ("Thêm sản phẩm", "Thêm dịch vụ", "Thêm Lưu ý").
  - Gán sự kiện bàn phím trực tiếp: Nhấn `Shift + Enter` để tự động thêm dòng mới vào danh sách hiện tại. Nhấn `Enter` để chèn dòng xuống dòng mềm (`<br>`) ngay trong mục hiện tại, tránh lỗi phân thẻ của trình duyệt.
- **Bộ điều chỉnh cỡ chữ trực quan (Font Sizers)**:
  - Thêm 4 thanh trượt tùy chỉnh kích cỡ chữ cho: Tên Gói, Đơn Giá, Danh Sách và Chân Trang (Footer) hiển thị trực tiếp trên sidebar điều khiển.
  - Hỗ trợ thay đổi trực quan theo thời gian thực và lưu trữ tự động vào `localStorage` của trình duyệt.
- **Đồng bộ thương hiệu**:
  - Đổi tên mục hướng dẫn ở chân Sidebar thành "Mẹo quản lý Báo giá:" cho phù hợp ngữ cảnh của phần mềm.
- **Xác minh & Triển khai**:
  - Kiểm thử đầy đủ, chụp 3 ảnh minh họa tính năng lưu vào brain folder.
  - Triển khai trực tiếp lên Vercel thành công tại địa chỉ [https://baogiahans.vercel.app](https://baogiahans.vercel.app).
  - Đẩy toàn bộ thay đổi lên Git của kho lưu trữ `hans-studio`.
- **Khắc phục lỗi Vercel 404 NOT_FOUND (baogiahans.vercel.app)**:
  - **Nguyên nhân**: Dự án `baogiahans` trên Vercel bị liên kết nhầm với Git repo `master-agent`, dẫn đến việc các bản build tự động từ Git push của `master-agent` đè lên deployment và gây lỗi 404 do thiếu tệp tin.
  - **Khắc phục**:
    1. Chạy `vercel deploy --prod --yes` trực tiếp từ thư mục `bao-gia` để khôi phục và cập nhật bản build chính xác cho production.
    2. Chuyển đổi liên kết Git của dự án Vercel sang đúng repo `hiephoangstudio-source/hans-studio`.
    3. Hướng dẫn anh Hiệp cấu hình lại `Root Directory` thành `bao-gia` trên Dashboard của Vercel để tránh lỗi lặp lại trong các bản build tự động sau này.

